export interface Report {
    fromDate: string;     // or Date if you prefer
    toDate: string;       // or Date if you prefer
    branchName: string;
    serviceName: string;
    employeeName: string;
}